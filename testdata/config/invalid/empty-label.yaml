apiVersion: labdns.dev/v1alpha1
kind: LabDNS
metadata:
  name: empty-label
spec:
  zones:
    - id: z
      name: lab.example.net.
      mode: overlay
      records:
        - id: a1
          owner: foo..lab.example.net.
          type: A
          values: [10.0.0.1]
